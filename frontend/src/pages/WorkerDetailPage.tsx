import { useQuery } from '@tanstack/react-query'
import { Activity, ArrowLeft, Cpu, HardHat, Radio, ShieldAlert, Thermometer, Wifi, Wind } from 'lucide-react'
import { Link, useParams } from 'react-router-dom'
import { Area, AreaChart, Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { api } from '../services/api'
import type { NearMiss, Worker } from '../types'
import { ErrorState, LoadingState } from '../components/common/States'
import { Panel } from '../components/common/Panel'
import { RiskBadge } from '../components/common/RiskBadge'
import { formatDate } from '../lib/utils'

interface WorkerDetail extends Worker { risk_factors: { factor: string; value: number }[]; near_misses: NearMiss[] }

export default function WorkerDetailPage() {
  const { workerId = '' } = useParams()
  const worker = useQuery({
    queryKey: ['worker', workerId],
    queryFn: () => api<WorkerDetail>(`/workers/${workerId}`),
    refetchInterval: 2_000,
    refetchIntervalInBackground: true,
  })
  const history = useQuery({
    queryKey: ['worker-history', workerId],
    queryFn: () => api<{ items: { date: string; score: number }[] }>(`/workers/${workerId}/risk-history`),
    refetchInterval: 10_000,
  })
  if (worker.isLoading) return <LoadingState />
  if (worker.isError || !worker.data) return <ErrorState retry={() => void worker.refetch()} />
  const data = worker.data
  return <div className="stack-lg wide-page worker-detail-page"><Link className="back-link" to="/workers"><ArrowLeft size={16}/> Kembali ke daftar pekerja</Link><div className="worker-hero"><div className="avatar large">{data.name.split(' ').map((p) => p[0]).slice(0, 2).join('')}</div><div className="worker-hero-copy"><span>{data.id} · {data.role}</span><h2>{data.name}</h2><p>{data.area} · Shift Pagi · 7 jam 12 menit aktif</p></div><div className="hero-risk"><span>Current Risk Score</span><strong>{data.online ? data.risk_score : '-'}</strong>{data.online ? <RiskBadge level={data.risk_level}/> : <span style={{ fontSize: '11px', color: 'var(--muted)', fontWeight: 600 }}>-</span>}</div></div><div className="status-strip" style={!data.online ? { opacity: 0.5 } : {}}><div><HardHat/><span>Smart Helmet<b>{data.online ? 'Online' : 'Offline'}</b></span></div><div><Radio/><span>Nearest Hazard<b>{data.online ? data.hazard_name : '-'}</b></span></div><div><Thermometer/><span>Environment<b>{data.online ? `${data.temperature}°C · ${data.humidity}%` : '-'}</b></span></div><div><Wind/><span>Air Quality<b>{data.online ? data.air_quality_level : '-'}</b></span></div><div><Activity/><span>Impact<b>{data.online ? `${data.impact_g.toFixed(1)}g · ${data.fall_detected ? 'Fall' : data.impact ? 'Hit' : 'Normal'}` : '-'}</b></span></div><div><Wifi/><span>Signal RSSI<b>{data.online ? `${data.rssi} dBm` : '-'}</b></span></div></div><div className="detail-grid"><Panel title="AI Pre-Danger Forecaster (Beta)" subtitle="Model prediktif dari Reksa AI v2" className="action-panel"><div className={`recommendation ${data.risk_level === 'CRITICAL' && data.online ? 'risk-bg-critical' : 'risk-bg-safe'}`}><Cpu size={32}/><div><strong>{data.risk_level === 'CRITICAL' && data.online ? '94% Probabilitas Insiden' : '12% Probabilitas Insiden'}</strong><p>{data.risk_level === 'CRITICAL' && data.online ? 'Berdasarkan trajectory 5 menit terakhir, ada pola bahaya ekstrem terdeteksi. Risiko tinggi dalam horizon 3 detik ke depan.' : 'Berdasarkan trajectory 5 menit terakhir, tidak ada pola bahaya tertunda yang terdeteksi. Aman dalam horizon 3 detik ke depan.'}</p></div></div><div className="source-line"><span>MODEL</span><b>Random Forest Classifier (v2.1)</b></div></Panel><Panel title="Recommended Supervisor Action" subtitle="Tindakan berdasarkan assessment terbaru" className="action-panel"><div className={`recommendation risk-bg-${data.risk_level.toLowerCase()}`}><ShieldAlert size={32}/><div><strong>{data.risk_level === 'CRITICAL' ? 'Tindakan Darurat Diperlukan' : data.risk_level === 'HIGH' ? 'Peringatan Bahaya Tinggi' : data.risk_level === 'MODERATE' ? 'Rekomendasi Sistem (Moderat)' : 'Status Aman - Rekomendasi Sistem'}</strong><p>{data.recommended_action}</p></div></div><div className="source-line"><span>SUMBER DATA</span><b>{data.calculation_source.replaceAll('_', ' ')}</b></div></Panel><Panel title="AI Priority Engine (Beta)" subtitle="Distribusi Prioritas Kejadian"><div className="distribution-wrap" style={{ height: '245px', display: 'flex', flexDirection: 'column', justifyContent: 'space-between', alignItems: 'stretch', width: '100%' }}>{data.near_misses.length === 0 ? <div className="empty-distribution" style={{ height: '100%', display: 'flex', alignItems: 'center', justifyContent: 'center', color: 'var(--muted)', fontSize: '14px', flexDirection: 'column', gap: '8px' }}><Cpu size={32} style={{ color: 'var(--muted)', opacity: 0.5 }} /><span>Belum ada event untuk dianalisis.</span></div> : <><div style={{ flex: 1, display: 'flex', flexDirection: 'column', justifyContent: 'center', gap: '14px', padding: '0 8px', width: '100%' }}><div style={{ display: 'flex', alignItems: 'center', gap: '14px', width: '100%' }}><div style={{ width: '42px', height: '42px', background: '#fef2f2', color: '#dc2626', borderRadius: '10px', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: '18px', fontWeight: 700, flexShrink: 0 }}>{data.near_misses.filter(e => e.risk_score >= 80).length}</div><div style={{ flex: 1, minWidth: 0 }}><div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: '6px' }}><strong style={{ fontSize: '13px', color: '#142236', whiteSpace: 'nowrap' }}>Kritis (HIGH)</strong><span style={{ fontSize: '11px', color: '#7b8ea9', whiteSpace: 'nowrap', marginLeft: '8px' }}>Butuh tindakan</span></div><div style={{ width: '100%', height: '6px', background: '#f1f5f9', borderRadius: '3px', overflow: 'hidden' }}><div style={{ width: `${(data.near_misses.filter(e => e.risk_score >= 80).length / data.near_misses.length) * 100}%`, height: '100%', background: '#f45454', borderRadius: '3px', transition: 'width 0.5s ease' }} /></div></div></div><div style={{ display: 'flex', alignItems: 'center', gap: '14px', width: '100%' }}><div style={{ width: '42px', height: '42px', background: '#fffbeb', color: '#d97706', borderRadius: '10px', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: '18px', fontWeight: 700, flexShrink: 0 }}>{data.near_misses.filter(e => e.risk_score >= 50 && e.risk_score < 80).length}</div><div style={{ flex: 1, minWidth: 0 }}><div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: '6px' }}><strong style={{ fontSize: '13px', color: '#142236', whiteSpace: 'nowrap' }}>Peringatan (MEDIUM)</strong><span style={{ fontSize: '11px', color: '#7b8ea9', whiteSpace: 'nowrap', marginLeft: '8px' }}>Evaluasi</span></div><div style={{ width: '100%', height: '6px', background: '#f1f5f9', borderRadius: '3px', overflow: 'hidden' }}><div style={{ width: `${(data.near_misses.filter(e => e.risk_score >= 50 && e.risk_score < 80).length / data.near_misses.length) * 100}%`, height: '100%', background: '#f6c350', borderRadius: '3px', transition: 'width 0.5s ease' }} /></div></div></div><div style={{ display: 'flex', alignItems: 'center', gap: '14px', width: '100%' }}><div style={{ width: '42px', height: '42px', background: '#f0fdf4', color: '#16a34a', borderRadius: '10px', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: '18px', fontWeight: 700, flexShrink: 0 }}>{data.near_misses.filter(e => e.risk_score < 50).length}</div><div style={{ flex: 1, minWidth: 0 }}><div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: '6px' }}><strong style={{ fontSize: '13px', color: '#142236', whiteSpace: 'nowrap' }}>Aman (LOW)</strong><span style={{ fontSize: '11px', color: '#7b8ea9', whiteSpace: 'nowrap', marginLeft: '8px' }}>Abaikan otomatis</span></div><div style={{ width: '100%', height: '6px', background: '#f1f5f9', borderRadius: '3px', overflow: 'hidden' }}><div style={{ width: `${(data.near_misses.filter(e => e.risk_score < 50).length / data.near_misses.length) * 100}%`, height: '100%', background: '#2dd4a3', borderRadius: '3px', transition: 'width 0.5s ease' }} /></div></div></div></div><div className="source-line" style={{ marginTop: '0', width: '100%' }}><span>MODEL</span><b>Logistic Regression (v2.1)</b></div></>}</div></Panel><Panel title="Risk Factor Summary" subtitle="Kontribusi dominan pada skor saat ini"><ResponsiveContainer width="100%" height={245}><BarChart data={data.risk_factors} layout="vertical" margin={{ left: 20 }}><CartesianGrid stroke="#ced9e9" horizontal={false}/><XAxis type="number" domain={[0, 100]} stroke="#7b8ea9"/><YAxis type="category" dataKey="factor" width={110} stroke="#7b8ea9" fontSize={11}/><Tooltip contentStyle={{ background: '#ffffff', border: '1px solid #ced9e9', borderRadius: 8, boxShadow: '0 4px 12px rgba(0,0,0,0.05)' }} itemStyle={{ color: '#142236', fontSize: 13, fontWeight: 600 }} labelStyle={{ color: '#7b8ea9', fontSize: 11, marginBottom: 4 }}/><Bar dataKey="value" fill="#4f9cf9" radius={[0, 5, 5, 0]}/></BarChart></ResponsiveContainer></Panel></div><Panel title="Risk History" subtitle="Perubahan skor risiko selama 14 hari"><ResponsiveContainer width="100%" height={260}><AreaChart data={history.data?.items ?? []}><defs><linearGradient id="workerRisk" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="#f6c350" stopOpacity={.35}/><stop offset="1" stopColor="#f6c350" stopOpacity={0}/></linearGradient></defs><CartesianGrid stroke="#ced9e9" strokeDasharray="3 3" vertical={false}/><XAxis dataKey="date" tickFormatter={(v: string) => v.slice(5)} stroke="#7b8ea9"/><YAxis stroke="#7b8ea9"/><Tooltip contentStyle={{ background: '#ffffff', border: '1px solid #ced9e9', borderRadius: 8, boxShadow: '0 4px 12px rgba(0,0,0,0.05)' }} itemStyle={{ color: '#142236', fontSize: 13, fontWeight: 600 }} labelStyle={{ color: '#7b8ea9', fontSize: 11, marginBottom: 4 }}/><Area dataKey="score" stroke="#f6c350" fill="url(#workerRisk)" strokeWidth={2}/></AreaChart></ResponsiveContainer></Panel><Panel title="Near-Miss History" subtitle={`${data.near_misses.length} kejadian terkait pekerja`}>{data.near_misses.length === 0 ? <div style={{ padding: '24px', textAlign: 'center', color: 'var(--muted)', fontSize: '14px' }}>Tidak ada riwayat Near-Miss untuk pekerja ini.</div> : <div className="table-wrap"><table><thead><tr><th>Timestamp</th><th>Hazard</th><th>RSSI</th><th>Duration</th><th>Risk</th><th>AI Priority</th><th>Status</th></tr></thead><tbody>{data.near_misses.map((event) => <tr key={event.id}><td>{formatDate(event.timestamp)}</td><td><strong>{event.hazard_name}</strong><small>{event.hazard_type}</small></td><td>{event.rssi} dBm</td><td>{event.duration.toFixed(1)} dtk</td><td><b>{event.risk_score}</b></td><td><span className={`ai-badge ${event.risk_score >= 80 ? 'high' : event.risk_score >= 50 ? 'medium' : 'low'}`} style={{ fontSize: '11px', padding: '4px 8px', borderRadius: '4px', fontWeight: 600, background: event.risk_score >= 80 ? '#fef2f2' : event.risk_score >= 50 ? '#fffbeb' : '#f0fdf4', color: event.risk_score >= 80 ? '#dc2626' : event.risk_score >= 50 ? '#d97706' : '#16a34a' }}>{event.risk_score >= 80 ? 'HIGH' : event.risk_score >= 50 ? 'MEDIUM' : 'LOW'}</span></td><td>{event.acknowledged ? 'Acknowledged' : 'Needs review'}</td></tr>)}</tbody></table></div>}</Panel></div>
}
